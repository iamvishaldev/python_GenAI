# range
total_orders = int(input("Enter number of chai orders: "))

for token in range(1, total_orders +1):
    print(f"☕ Serving chai to Token #{token}")


# For each token:

# If token is even → print "VIP Order"

# Else → "Regular Order"

for token in range(0,total_orders+1):
    print(f"☕ Serving chai to Token #{token}")